from t02_10 import f

def g(n):
    sum = 0                     #O(1)
    for i in range(1, n + 1):   #O(n)
        sum = sum + i + f(i)    #O(n^2)
    return sum                  #O(1)
#O(n^2)

"""
Результатом виконання наведеної функції є наступна сума:
sum{k=1}^{n}[k+f(k)]

Оптимізувати можна наступним чином:

def g(n):
    return (n*(n+1)*(n+5))/6 #O(1)
В цьому випадку асимптотична оцінка буде: O(1) 
"""
